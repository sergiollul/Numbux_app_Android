<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Uninstall‐dialog block duration -->
    <EditTextPreference
        android:key="key_uninstall_block_timeout"
        android:title="Uninstall block timeout"
        android:summary="Milliseconds to block touches when the uninstall dialog opens"
        android:dialogTitle="Set uninstall block timeout (ms)"
        android:defaultValue="3000"
        android:inputType="number" />

    <!-- Master on/off switch for blocking -->
    <SwitchPreferenceCompat
        android:key="blocking_enabled"
        android:title="Enable blocking"
        android:summary="Turn accessibility blocking on or off"
        android:defaultValue="true" />

</PreferenceScreen>
